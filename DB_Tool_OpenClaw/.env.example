# Database connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Connection pool settings (optional)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# Application settings (optional)
APP_TITLE=DB Tool OpenClaw
APP_VERSION=1.0.0
LOG_LEVEL=INFO
